<Page x:Class="Frontend.PlayerGameStatsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Box Score" Background="#0D0D0D">

    <Page.Resources>
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="RowBackground" Value="#1A1A1A"/>
            <Setter Property="AlternatingRowBackground" Value="#1E1E1E"/>
            <Setter Property="BorderBrush" Value="#2E2E2E"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#2E2E2E"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="Foreground" Value="#999999"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Padding" Value="10,12"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#2E2E2E"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,25">
            <Button Content="← BACK TO SCHEDULE" Click="Back_Click" 
                    Background="Transparent" Foreground="#555555" BorderThickness="0" 
                    HorizontalAlignment="Left" Cursor="Hand" FontSize="11" FontWeight="Bold" Margin="0,0,0,15"/>

            <TextBlock x:Name="GameHeader" Text="GAME BOX SCORE" FontSize="28" FontWeight="Bold" Foreground="White" HorizontalAlignment="Left"/>
            <Rectangle Height="1" Fill="#2E2E2E" Margin="0,15,0,0" HorizontalAlignment="Stretch"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <Button x:Name="HomeBtn" Content="HOME" Click="ToggleTeam_Click" 
                    Width="120" Height="40" Background="#F05A28" Foreground="White" 
                    FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
            <Button x:Name="AwayBtn" Content="AWAY" Click="ToggleTeam_Click" 
                    Width="120" Height="40" Background="#242424" Foreground="#999999" 
                    FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
        </StackPanel>

        <DataGrid x:Name="StatsGrid" Grid.Row="2" VerticalAlignment="Top">
            <DataGrid.Columns>

                <DataGridTemplateColumn Header="PLAYER" Width="250">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="{Binding JerseyNumber}" Foreground="#555555" Width="25" VerticalAlignment="Center" FontWeight="Bold"/>
                                <TextBlock Text="{Binding PlayerName}" Foreground="White" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding Position}" Foreground="#555555" Margin="8,0,0,0" FontSize="10" VerticalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="MIN" Binding="{Binding PlayingTime}" Width="50"/>

                <DataGridTextColumn Header="PTS" Binding="{Binding Points}" Width="60">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="HorizontalAlignment" Value="Left"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="FG" Binding="{Binding FieldGoalsDisplay}" Width="50"/>
                <DataGridTextColumn Header="3PT" Binding="{Binding ThreePointersDisplay}" Width="50"/>
                <DataGridTextColumn Header="REB" Binding="{Binding Rebounds}" Width="50"/>
                <DataGridTextColumn Header="AST" Binding="{Binding Assists}" Width="50"/>
                <DataGridTextColumn Header="STL" Binding="{Binding Steals}" Width="50"/>
                <DataGridTextColumn Header="BLK" Binding="{Binding Blocks}" Width="50"/>
                <DataGridTextColumn Header="TO" Binding="{Binding Turnovers}" Width="50"/>
                <DataGridTextColumn Header="PF" Binding="{Binding PersonalFouls}" Width="50"/>

            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>