<Page x:Class="Frontend.PlayerGameStatsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Box Score"
      Background="#0D0D0D">

    <Page.Resources>
        <!-- Override system selection colors to stay dark -->
        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#1F2638"/>
        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="White"/>
        <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="#1A1A1A"/>
        <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="White"/>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="RowBackground" Value="#1A1A1A"/>
            <Setter Property="AlternatingRowBackground" Value="#1E1E1E"/>
            <Setter Property="BorderBrush" Value="#2E2E2E"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#2E2E2E"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowHeight" Value="40"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="Foreground" Value="#999999"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Padding" Value="10,12"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#2E2E2E"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#1F2638"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#222222"/>
                </Trigger>
                <DataTrigger Binding="{Binding IsEditing}" Value="True">
                    <Setter Property="Background" Value="#1A2030"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBox">
            <EventSetter Event="GotFocus" Handler="StatBox_GotFocus"/>
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#FF8C5A"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Grid Margin="30">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,0,20">
            <Button Content="← BACK TO SCHEDULE"
                    Click="Back_Click"
                    Style="{StaticResource GhostButton}"
                    HorizontalAlignment="Left"
                    FontSize="11"
                    FontWeight="Bold"
                    Margin="0,0,0,10"/>
            <Grid>
                <TextBlock x:Name="GameHeader"
                           FontSize="28"
                           FontWeight="Bold"
                           Foreground="White"/>
                <Button x:Name="AutoFillButton"
                        Content="GENERATE STATS"
                        Click="AutoFillStats_Click"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Cursor="Hand"
                        Foreground="White"
                        FontSize="11"
                        FontWeight="Bold"
                        Padding="20,10"
                        Visibility="Collapsed">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Bd" Background="#F05A28" CornerRadius="4"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="#D94E22"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
            <Rectangle Height="1"
                       Fill="#2E2E2E"
                       Margin="0,15,0,0"/>
        </StackPanel>

        <!-- Home + Away Box Scores -->
        <ScrollViewer Grid.Row="1"
                      Grid.Column="0"
                      VerticalScrollBarVisibility="Disabled"
                      HorizontalScrollBarVisibility="Disabled">
            <StackPanel>

                <!-- Home Team -->
                <TextBlock x:Name="HomeTeamLabel"
                           Foreground="#6E6E6E"
                           FontSize="16"
                           FontWeight="Bold"
                           Margin="0,0,0,10"/>

                <DataGrid x:Name="HomeStatsGrid" Margin="0,0,0,25"
                          MouseDoubleClick="StatsGrid_DoubleClick">
                    <DataGrid.Columns>
                        <DataGridTemplateColumn Header="PLAYER" Width="180">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding PlayerName}" Foreground="White" FontWeight="SemiBold"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="MIN" Width="55">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <TextBlock Text="{Binding PlayingTime}" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                   Visibility="{Binding IsEditing, Converter={StaticResource InverseBoolToVis}}"/>
                                        <TextBox Text="{Binding EditMinutes, UpdateSourceTrigger=PropertyChanged}"
                                                 Background="#2A2A2A" Foreground="White" BorderBrush="#F05A28" BorderThickness="1"
                                                 TextAlignment="Center" MaxLength="3" VerticalAlignment="Center"
                                                 Visibility="{Binding IsEditing, Converter={StaticResource BoolToVis}}"/>
                                    </Grid>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="PTS" Width="55">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Points}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="FG" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <TextBlock Text="{Binding FieldGoalsDisplay}" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                   Visibility="{Binding IsEditing, Converter={StaticResource InverseBoolToVis}}"/>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                    Visibility="{Binding IsEditing, Converter={StaticResource BoolToVis}}">
                                            <TextBox Text="{Binding EditFGMade, UpdateSourceTrigger=PropertyChanged}"
                                                     Width="36" Background="#2A2A2A" Foreground="White"
                                                     BorderBrush="#F05A28" BorderThickness="1" TextAlignment="Center" MaxLength="3"/>
                                            <TextBlock Text="/" Foreground="#555" Margin="2,0" VerticalAlignment="Center"/>
                                            <TextBox Text="{Binding EditFGTaken, UpdateSourceTrigger=PropertyChanged}"
                                                     Width="36" Background="#2A2A2A" Foreground="White"
                                                     BorderBrush="#F05A28" BorderThickness="1" TextAlignment="Center" MaxLength="3"/>
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="3PT" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <TextBlock Text="{Binding ThreePointersDisplay}" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                   Visibility="{Binding IsEditing, Converter={StaticResource InverseBoolToVis}}"/>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                    Visibility="{Binding IsEditing, Converter={StaticResource BoolToVis}}">
                                            <TextBox Text="{Binding EditThreeMade, UpdateSourceTrigger=PropertyChanged}"
                                                     Width="36" Background="#2A2A2A" Foreground="White"
                                                     BorderBrush="#F05A28" BorderThickness="1" TextAlignment="Center" MaxLength="3"/>
                                            <TextBlock Text="/" Foreground="#555" Margin="2,0" VerticalAlignment="Center"/>
                                            <TextBox Text="{Binding EditThreeTaken, UpdateSourceTrigger=PropertyChanged}"
                                                     Width="36" Background="#2A2A2A" Foreground="White"
                                                     BorderBrush="#F05A28" BorderThickness="1" TextAlignment="Center" MaxLength="3"/>
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="REB" Width="55">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <TextBlock Text="{Binding Rebounds}" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                   Visibility="{Binding IsEditing, Converter={StaticResource InverseBoolToVis}}"/>
                                        <TextBox Text="{Binding EditRebounds, UpdateSourceTrigger=PropertyChanged}"
                                                 Background="#2A2A2A" Foreground="White" BorderBrush="#F05A28" BorderThickness="1"
                                                 TextAlignment="Center" MaxLength="3" VerticalAlignment="Center"
                                                 Visibility="{Binding IsEditing, Converter={StaticResource BoolToVis}}"/>
                                    </Grid>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="AST" Width="55">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <TextBlock Text="{Binding Assists}" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                   Visibility="{Binding IsEditing, Converter={StaticResource InverseBoolToVis}}"/>
                                        <TextBox Text="{Binding EditAssists, UpdateSourceTrigger=PropertyChanged}"
                                                 Background="#2A2A2A" Foreground="White" BorderBrush="#F05A28" BorderThickness="1"
                                                 TextAlignment="Center" MaxLength="3" VerticalAlignment="Center"
                                                 Visibility="{Binding IsEditing, Converter={StaticResource BoolToVis}}"/>
                                    </Grid>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="TO" Width="55">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <TextBlock Text="{Binding Turnovers}" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                   Visibility="{Binding IsEditing, Converter={StaticResource InverseBoolToVis}}"/>
                                        <TextBox Text="{Binding EditTurnovers, UpdateSourceTrigger=PropertyChanged}"
                                                 Background="#2A2A2A" Foreground="White" BorderBrush="#F05A28" BorderThickness="1"
                                                 TextAlignment="Center" MaxLength="3" VerticalAlignment="Center"
                                                 Visibility="{Binding IsEditing, Converter={StaticResource BoolToVis}}"/>
                                    </Grid>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Away Team -->
                <TextBlock x:Name="AwayTeamLabel"
                           Foreground="#6E6E6E"
                           FontSize="16"
                           FontWeight="Bold"
                           Margin="10,10,0,10"/>

                <DataGrid x:Name="AwayStatsGrid"
                          MouseDoubleClick="StatsGrid_DoubleClick">
                    <DataGrid.Columns>
                        <DataGridTemplateColumn Header="PLAYER" Width="180">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding PlayerName}" Foreground="White" FontWeight="SemiBold"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="MIN" Width="55">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <TextBlock Text="{Binding PlayingTime}" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                   Visibility="{Binding IsEditing, Converter={StaticResource InverseBoolToVis}}"/>
                                        <TextBox Text="{Binding EditMinutes, UpdateSourceTrigger=PropertyChanged}"
                                                 Background="#2A2A2A" Foreground="White" BorderBrush="#F05A28" BorderThickness="1"
                                                 TextAlignment="Center" MaxLength="3" VerticalAlignment="Center"
                                                 Visibility="{Binding IsEditing, Converter={StaticResource BoolToVis}}"/>
                                    </Grid>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="PTS" Width="55">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Points}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="FG" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <TextBlock Text="{Binding FieldGoalsDisplay}" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                   Visibility="{Binding IsEditing, Converter={StaticResource InverseBoolToVis}}"/>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                    Visibility="{Binding IsEditing, Converter={StaticResource BoolToVis}}">
                                            <TextBox Text="{Binding EditFGMade, UpdateSourceTrigger=PropertyChanged}"
                                                     Width="36" Background="#2A2A2A" Foreground="White"
                                                     BorderBrush="#F05A28" BorderThickness="1" TextAlignment="Center" MaxLength="3"/>
                                            <TextBlock Text="/" Foreground="#555" Margin="2,0" VerticalAlignment="Center"/>
                                            <TextBox Text="{Binding EditFGTaken, UpdateSourceTrigger=PropertyChanged}"
                                                     Width="36" Background="#2A2A2A" Foreground="White"
                                                     BorderBrush="#F05A28" BorderThickness="1" TextAlignment="Center" MaxLength="3"/>
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="3PT" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <TextBlock Text="{Binding ThreePointersDisplay}" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                   Visibility="{Binding IsEditing, Converter={StaticResource InverseBoolToVis}}"/>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                    Visibility="{Binding IsEditing, Converter={StaticResource BoolToVis}}">
                                            <TextBox Text="{Binding EditThreeMade, UpdateSourceTrigger=PropertyChanged}"
                                                     Width="36" Background="#2A2A2A" Foreground="White"
                                                     BorderBrush="#F05A28" BorderThickness="1" TextAlignment="Center" MaxLength="3"/>
                                            <TextBlock Text="/" Foreground="#555" Margin="2,0" VerticalAlignment="Center"/>
                                            <TextBox Text="{Binding EditThreeTaken, UpdateSourceTrigger=PropertyChanged}"
                                                     Width="36" Background="#2A2A2A" Foreground="White"
                                                     BorderBrush="#F05A28" BorderThickness="1" TextAlignment="Center" MaxLength="3"/>
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="REB" Width="55">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <TextBlock Text="{Binding Rebounds}" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                   Visibility="{Binding IsEditing, Converter={StaticResource InverseBoolToVis}}"/>
                                        <TextBox Text="{Binding EditRebounds, UpdateSourceTrigger=PropertyChanged}"
                                                 Background="#2A2A2A" Foreground="White" BorderBrush="#F05A28" BorderThickness="1"
                                                 TextAlignment="Center" MaxLength="3" VerticalAlignment="Center"
                                                 Visibility="{Binding IsEditing, Converter={StaticResource BoolToVis}}"/>
                                    </Grid>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="AST" Width="55">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <TextBlock Text="{Binding Assists}" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                   Visibility="{Binding IsEditing, Converter={StaticResource InverseBoolToVis}}"/>
                                        <TextBox Text="{Binding EditAssists, UpdateSourceTrigger=PropertyChanged}"
                                                 Background="#2A2A2A" Foreground="White" BorderBrush="#F05A28" BorderThickness="1"
                                                 TextAlignment="Center" MaxLength="3" VerticalAlignment="Center"
                                                 Visibility="{Binding IsEditing, Converter={StaticResource BoolToVis}}"/>
                                    </Grid>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="TO" Width="55">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <TextBlock Text="{Binding Turnovers}" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                   Visibility="{Binding IsEditing, Converter={StaticResource InverseBoolToVis}}"/>
                                        <TextBox Text="{Binding EditTurnovers, UpdateSourceTrigger=PropertyChanged}"
                                                 Background="#2A2A2A" Foreground="White" BorderBrush="#F05A28" BorderThickness="1"
                                                 TextAlignment="Center" MaxLength="3" VerticalAlignment="Center"
                                                 Visibility="{Binding IsEditing, Converter={StaticResource BoolToVis}}"/>
                                    </Grid>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </ScrollViewer>

        <!-- Right Side: Game Stats Summary -->
        <Border Grid.Row="1" Grid.Column="1"
                Background="#1A1A1A" BorderBrush="#2E2E2E"
                BorderThickness="1" CornerRadius="8"
                Padding="20" VerticalAlignment="Top"
                Margin="20,0,0,0">
            <StackPanel>
                <TextBlock Text="GAME STATS SUMMARY" FontSize="12" FontWeight="Bold"
                           Foreground="#F05A28" Margin="0,0,0,4"/>
                <TextBlock Text="Averages per player this game"
                           FontSize="10" Foreground="#555555" Margin="0,0,0,16"/>

                <Rectangle Height="1" Fill="#2E2E2E" Margin="0,0,0,16"/>

                <!-- Points -->
                <Grid Margin="0,0,0,14">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="60"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="AVG Points" FontSize="13"
                               Foreground="#999999"/>
                    <TextBlock Grid.Column="1" x:Name="AvgPointsText"
                               FontSize="13" Foreground="White" FontWeight="Bold"
                               HorizontalAlignment="Center"/>
                </Grid>

                <!-- Rebounds -->
                <Grid Margin="0,0,0,14">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="60"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="AVG Rebounds" FontSize="13"
                               Foreground="#999999"/>
                    <TextBlock Grid.Column="1" x:Name="AvgReboundsText"
                               FontSize="13" Foreground="White" FontWeight="Bold"
                               HorizontalAlignment="Center"/>
                </Grid>

                <!-- Assists -->
                <Grid Margin="0,0,0,14">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="60"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="AVG Assists" FontSize="13"
                               Foreground="#999999"/>
                    <TextBlock Grid.Column="1" x:Name="AvgAssistsText"
                               FontSize="13" Foreground="White" FontWeight="Bold"
                               HorizontalAlignment="Center"/>
                </Grid>

                <!-- Turnovers -->
                <Grid Margin="0,0,0,14">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="60"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="AVG Turnovers" FontSize="13"
                               Foreground="#999999"/>
                    <TextBlock Grid.Column="1" x:Name="AvgTurnoversText"
                               FontSize="13" Foreground="White" FontWeight="Bold"
                               HorizontalAlignment="Center"/>
                </Grid>

            </StackPanel>
        </Border>
    </Grid>
</Page>